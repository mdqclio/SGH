# Sesión 2026-05-19

## Migraciones de schema aplicadas

### add_comision_carreras_and_sponsors_to_clubs
Aplicada vía MCP (apply_migration).

```sql
ALTER TABLE clubs
  ADD COLUMN IF NOT EXISTS comision_carreras JSONB DEFAULT '[]'::jsonb,
  ADD COLUMN IF NOT EXISTS sponsors JSONB DEFAULT '[]'::jsonb;
```

Formato esperado:
- `comision_carreras`: `[{"cargo":"Presidente","nombre":"Juan Pérez"}, ...]`
- `sponsors`: `[{"nombre":"YPF","logo_url":"https://..."}, ...]`

**Motivo:** Datos para el futuro Programa Oficial estilo revista. Editables desde el modal "Editar Hipódromo" en admin.html.

---

## Cambios en el frontend

- **admin.html**: ABM de Comisión de Carreras y Sponsors dentro del modal de edición de club. Funciones `renderComisionRow`, `renderSponsorRow`, `addComisionRow`, `addSponsorRow`, `readComisionFromForm`, `readSponsorsFromForm`. Integradas en `openEdit()` y `saveEdit()`.

---

## Auditoría de mismatches admin.html ↔ schema real de clubs

### Columnas reales en `clubs` (relevantes para admin.html)
`id`, `nombre`, `sigla`, `razon_social`, `cuit`, `domicilio`, `localidad`, `provincia`, `telefono`, `email`, `logo_url`, `activo`, `pais`, `auditoria_retencion_meses`, `comision_carreras`, `sponsors`

### Renombres aplicados

| Campo usado en admin.html | Columna real | Dónde |
|---|---|---|
| `direccion` | `domicilio` | insert, update, render, openEdit *(fix previo)* |
| `telefono_contacto` | `telefono` | insert, update, render, openEdit |
| `email_contacto` | `email` | insert, update, render, openEdit |

### Campos sin columna equivalente (TODO)

| Campo en admin.html | Estado |
|---|---|
| `nombre_contacto` | Input en modal pero sin columna en DB. No se persiste. Marcado con TODO en saveCreate y saveEdit. |
| `tel_contacto` | Input en modal pero sin columna en DB. No se persiste. Marcado con TODO en saveEdit. |

Decisión pendiente: crear columnas `nombre_contacto` / `tel_contacto` en DB, o eliminar los inputs del HTML.

---

## Migraciones adicionales — apuestas

### add_apuestas_simples_and_combinadas
Aplicada vía MCP (apply_migration).

```sql
ALTER TABLE clubs
  ADD COLUMN IF NOT EXISTS apuestas_simples TEXT[] DEFAULT ARRAY[]::TEXT[];

ALTER TABLE reuniones
  ADD COLUMN IF NOT EXISTS apuestas_combinadas JSONB DEFAULT '[]'::jsonb;
```

Formato esperado:
- `clubs.apuestas_simples`: `['Ganador','Placé','Exacta','Trifecta']`
- `reuniones.apuestas_combinadas`: `[{"nombre":"Doblete","carreras":"3 y 4"}, {"nombre":"Quíntuple","carreras":"2 a 6"}]`

**Motivo:** Datos para el futuro Programa Oficial. Apuestas simples editables en admin.html (modal "Editar Hipódromo"). Apuestas combinadas editables en reuniones.html (modal Nueva/Editar Reunión).

### Auditoría de reuniones.html ↔ schema real de reuniones

Columnas reales: `id, club_id, hipodromo_id, numero, fecha, tipo, estado, condicion_pista, tiempo_clima, observaciones, creado_por, created_at, updated_at, hora_cierre_ratificacion, apuestas_combinadas`

**Sin mismatches.** Todos los campos persistidos en reuniones.html coinciden con el schema real.

---

## Migraciones adicionales — rediseño PDF carta de llamados

### add_club_social_disclaimers_and_reunion_fechas
Aplicada vía MCP (apply_migration).

```sql
ALTER TABLE clubs
  ADD COLUMN IF NOT EXISTS disclaimer_importante TEXT,
  ADD COLUMN IF NOT EXISTS disclaimer_nota TEXT,
  ADD COLUMN IF NOT EXISTS website TEXT,
  ADD COLUMN IF NOT EXISTS instagram TEXT,
  ADD COLUMN IF NOT EXISTS facebook TEXT,
  ADD COLUMN IF NOT EXISTS tiktok TEXT,
  ADD COLUMN IF NOT EXISTS twitter_x TEXT,
  ADD COLUMN IF NOT EXISTS youtube TEXT;

ALTER TABLE reuniones
  ADD COLUMN IF NOT EXISTS fechas_inscripciones TEXT,
  ADD COLUMN IF NOT EXISTS fechas_forfaits TEXT,
  ADD COLUMN IF NOT EXISTS fechas_compromiso_montas TEXT;
```

**Motivo:** Rediseño completo del PDF de carta de llamados estilo Hipódromo de Dolores. Disclaimers, redes sociales y sponsors del club en admin.html. Fechas operativas de la reunión en reuniones.html.

### Cambios en el frontend
- **carta-llamados.html**: rediseño completo del PDF — cajas de carrera con caption de categoría, bono inline en rojo, bloques de novedades, disclaimers, fechas, secretaría con redes sociales, y logos de sponsors. Fix de bug `$$formatMonto` (doble signo $) en render de pantalla y print.
- **admin.html**: campos disclaimer_importante, disclaimer_nota, website, instagram, facebook, tiktok, twitter_x, youtube en el modal de edición de club.
- **reuniones.html**: campos fechas_inscripciones, fechas_forfaits, fechas_compromiso_montas en el modal de edición de reunión.

---

## Migración — apuestas movidas de club-level a carrera-level

### apuestas_carrera_level
Aplicada vía MCP (apply_migration).

```sql
ALTER TABLE carreras ADD COLUMN IF NOT EXISTS apuestas TEXT[] DEFAULT ARRAY[]::TEXT[];
ALTER TABLE clubs DROP COLUMN IF EXISTS apuestas_simples;
```

**Motivo:** Las apuestas simples son específicas de cada carrera (una puede habilitar Ganador+Exacta, otra solo Ganador), no del club completo. Se mueve `apuestas_simples` de `clubs` a `carreras.apuestas`. Las combinadas siguen siendo por reunión (`reuniones.apuestas_combinadas`).

**Cambios en frontend:**
- `admin.html`: eliminada la sección "Apuestas simples ofrecidas" y sus funciones.
- `carta-llamados.html`: agregado campo "Apuestas habilitadas" en el modal de cada turno, persistido en `carreras.apuestas`.
- `programa.html`: modal de Apuestas actualizado — combinadas editables arriba, por-carrera read-only abajo con aviso de edición en Carta de Llamados.

---

## Migración — comisariato per-reunión

### add_comisariato_to_reuniones
Aplicada vía MCP (apply_migration).

```sql
ALTER TABLE reuniones ADD COLUMN IF NOT EXISTS comisariato JSONB DEFAULT '[]'::jsonb;
```

Formato: `[{"cargo":"Presidente del Comisariato","nombre":"Antonio Carlos Iguacel"}, {"cargo":"Cronometrista","nombre":"Truppa Roberto Angel"}]`

**Motivo:** El comisariato cambia por reunión (los stewards del día), a diferencia de la Comisión de Carreras que es fija del club. Editable desde reuniones.html y desde el modal de Comisión en programa.html.

---

## Refactor — comisariato a club-level

### comisariato_club_level
Aplicada vía MCP (apply_migration).

```sql
ALTER TABLE clubs ADD COLUMN IF NOT EXISTS comisariato JSONB DEFAULT '[]'::jsonb;

UPDATE clubs c
SET comisariato = sub.comisariato
FROM (
  SELECT DISTINCT ON (r.club_id) r.club_id, r.comisariato
  FROM reuniones r
  WHERE r.comisariato IS NOT NULL
    AND jsonb_array_length(r.comisariato) > 0
  ORDER BY r.club_id, r.fecha DESC NULLS LAST
) sub
WHERE c.id = sub.club_id
  AND (c.comisariato IS NULL OR jsonb_array_length(c.comisariato) = 0);

ALTER TABLE reuniones DROP COLUMN IF EXISTS comisariato;
```

**Motivo:** El comisariato es fijo del hipódromo (igual que la Comisión de Carreras), no cambia por reunión. Dato existente migrado del registro más reciente por club.

**Cambios en frontend:**
- `admin.html`: sección "Comisariato (stewards del hipódromo)" editable en el modal de edición de club.
- `programa.html`: `openComisionModal()` muestra ambas listas (comisión y comisariato) read-only desde `clubData`. Removidas funciones editables y botón Guardar del modal.
- `reuniones.html`: eliminada toda referencia al comisariato (HTML, funciones, payload).

---

## Refactor — eliminar apuestas combinadas, edición por carrera centralizada en Programa

### drop_apuestas_combinadas
Aplicada vía MCP (apply_migration).

```sql
ALTER TABLE reuniones DROP COLUMN IF EXISTS apuestas_combinadas;
```

**Motivo:** Simplificación del modelo: un único nivel de apuestas por carrera (`carreras.apuestas TEXT[]`). Las apuestas combinadas eran redundantes y confusas. La edición se centraliza en el modal de Apuestas de programa.html.

**Cambios en frontend:**
- `carta-llamados.html`: eliminada la sección "Apuestas habilitadas" del modal de turno.
- `reuniones.html`: eliminada la sección "Apuestas combinadas" del modal y todas sus funciones.
- `programa.html`: modal de Apuestas rediseñado — lista de inputs por carrera (separados por coma), guardado bulk vía `Promise.all`. Re-render del programa al guardar para actualizar chips.

---

## Admin accesible para admin-de-hipódromo (sin super_admin)

**Commit:** `842d354 feat(admin): permitir al admin de cada hipódromo editar sus datos blandos`

Admin.html detecta el rol del usuario logueado. Si es `secretario_carreras` (admin de un hipódromo), muestra únicamente el card de su club con los campos editables (comisión, sponsors, comisariato, disclaimers, redes). Si es `super_admin`, muestra todos los clubs con funciones de alta/baja. Los inputs de nombre, sigla, CUIT y campos fundacionales no son editables por el admin-de-hipódromo — solo los "datos blandos".

---

## Hora cierre ratificación editable desde modal de reuniones

**Commit:** `b5e77c6 feat(reuniones): agregar campo hora cierre ratificación en el modal`

La columna `reuniones.hora_cierre_ratificacion TIME NOT NULL DEFAULT '12:00:00'` ya existía en la DB (creada en sesión anterior junto con ratificacion.html). En esta sesión se agregó el input `<input type="time">` al modal de nueva/editar reunión en reuniones.html para que la secretaría pueda cambiarlo sin acceso al SQL Editor. El campo se persiste en el payload de upsert de la reunión.

---

## Navegación con reunión activa en localStorage

**Commit:** `ede9ea2 feat(navegacion): persistir reunión activa en localStorage para que Programa abra automáticamente la última usada`

**Patrón implementado:**
1. `reuniones.html`: botón 📍 **Activar** en cada fila → `localStorage.setItem('sgh_active_reunion_id', reunionId)`.
2. `programa.html`: al cargar, si no hay `?reunion_id=` en la URL, lee `sgh_active_reunion_id` de localStorage. Si tampoco está, ejecuta query para traer la próxima reunión por fecha del club del usuario.
3. El selector de reunión en programa.html actualiza el localStorage al cambiar.

**Sin migración SQL** — es puramente frontend.
