# ISSUE-056 — merge a `main` (anular_recibo + protocolo de informes)

- **Fecha**: 2026-08-30
- **`main` después del merge**: `80839ff65a804b2b1843411ea93284891c85e74a`
- **`main` antes**: `cbeeee8`
- **Proyecto**: `unlhcuanfrtpatoipwve`
- **Antecedentes**: `docs/diagnosticos/2026-08-30_anular-recibo-plan.md` ·
  `docs/diagnosticos/2026-08-30_anular-recibo-resultados.md` ·
  `docs/diagnosticos/2026-08-30_anular-recibo-estado-post-corte.md`

## SHAs de los dos merges

| Merge | SHA | Rama |
|---|---|---|
| ISSUE-056 — `anular_recibo` v1 | **`34f6e83`** | `feat/anular-recibo` |
| Protocolo de informes | **`80839ff`** | `chore/protocolo-informes-salidas` |

`80839ff` es además el HEAD de `main`.

**Por qué urgía el primero**: la migración `20260830025830_anular_recibo_v1` ya estaba
aplicada en producción y `main` no tenía los archivos. Había drift entre la base y el
repo: la función existía en prod sin fuente versionada en la rama que se deploya.

---

## Guards

```
$ pwd
/home/clio/dev/SGH
```

```sql
SELECT (SELECT count(*) FROM spcs) AS spcs, current_database() AS db;
```
```json
[{"spcs":181,"db":"postgres"}]
```

ref del proyecto: `unlhcuanfrtpatoipwve`. Los tres guards dan.

---

## 1 · Decisiones tomadas (cierre de las preguntas abiertas del doc anterior)

| # | Pregunta abierta | Decisión | Hecho |
|---|---|---|---|
| 1 | `club_secuencias` de Dolores en 32 | **Queda en 32.** Los números de recibo son ilimitados y gastar algunos en pruebas no es problema (Fede). No hay hueco visible para Valeria: nunca vio los recibos 4 a 32, así que para ella la numeración arranca en **33** | No se tocó |
| 2 | `probe_recibo_pie_cobrador.mjs:175` no restaura la secuencia | **Arreglar** — no por el número sino porque el drift crecía hasta el 20/09 | ✅ assert R6 |
| 3 | Comentario falso en el restore de `probe_anular_recibo.mjs` | **Arreglar** — un comentario falso es peor que no tenerlo, el próximo que lo lea va a confiar | ✅ reescrito |
| 4 | UI de anulación | **Entrega siguiente** | ISSUE-056 → 🟡 PARCIAL |

---

## 2 · El fix de `probe_recibo_pie_cobrador.mjs`

Tres cambios. El comentario que decía lo contrario:

```diff
-    // club_secuencias NO se snapshotea ni se restaura: los números de recibo son ilimitados y
-    // quemar algunos en pruebas no es problema (decisión del usuario, 2026-08-28). El probe deja
-    // el correlativo adelantado en 2, a propósito.
+    // El probe emite 2 recibos reales, así que mueve el correlativo de Dolores. Se snapshotea
+    // arriba y se restaura en el finally (R6). Antes no se restauraba —"los números de recibo son
+    // ilimitados"— y el drift se acumulaba corrida a corrida: 28 → 32 en una sola noche
+    // (2026-08-30). El número no lo ve nadie, pero era el único probe del set que no lo devolvía.
```

El snapshot, junto al de líneas:

```js
const seqAntes = (await sb.from('club_secuencias')
  .select('ultimo_numero').eq('club_id', CLUB_ID).eq('tipo', 'recibo').maybeSingle()).data?.ultimo_numero ?? null;
```

Y el restore en el `finally`, con su assert:

```js
// ── R6 — club_secuencias ──────────────────────────────────────────────
// Borrar los recibos NO devuelve el correlativo: fn_siguiente_recibo es un contador
// monótono en club_secuencias, no un MAX+1. Hay que reponerlo a mano.
if (seqAntes === null) {
  await sb.from('club_secuencias').delete().eq('club_id', CLUB_ID).eq('tipo', 'recibo');
} else {
  await sb.from('club_secuencias').update({ ultimo_numero: seqAntes })
    .eq('club_id', CLUB_ID).eq('tipo', 'recibo');
}
const seqDespues = (await sb.from('club_secuencias')
  .select('ultimo_numero').eq('club_id', CLUB_ID).eq('tipo', 'recibo').maybeSingle()).data?.ultimo_numero ?? null;
ok('R6 club_secuencias de Dolores devuelto a donde estaba',
   seqDespues === seqAntes, `antes=${seqAntes} después=${seqDespues}`);
```

`?? null` y no `|| null`: si la fila existiera con `ultimo_numero = 0`, `||` lo trataría
como ausente y el restore borraría la fila en vez de reponerla en 0.

### Corrida de verificación contra prod — salida cruda completa

```
$ set -a; . ./.env; set +a
$ node tests/probe_recibo_pie_cobrador.mjs
[snapshot] 76 líneas del sandbox fotografiadas por estado · club_secuencias=32
[presupuesto] fila=5.61mm  fijo=87.6mm  → entran ~31 filas por hoja
[fixtures] liq=59e4f245-128d-4cdd-a738-4a548f475759  6 líneas caso Fede + 2 para transferencia

──────── RESULTADO ────────
✅ a1 .recibo-copia ya NO declara min-height:100vh
✅ a2 la firma ya NO usa margin-top:auto (no queda anclada al fondo de la caja)
✅ a3 .recibo-pie tiene break-inside: avoid
✅ a4 .recibo-pie tiene page-break-inside: avoid (legacy)
✅ a5 body resetea margin en impresión  (margin="0")
✅ a6 las copias se separan con break-after: page
✅ b1 el recibo de 6 líneas entra en una hoja (121mm de 267mm)
✅ b2 entra con holgura, no al filo (usa <70% de la hoja)  (45%)
✅ b3 el caso extremo de 20 líneas sigue entrando en una hoja  (200mm)
✅ d1 el select trae titular + 2 apoderados + "Otro" (4 opciones)  (n=4)
✅ d2 la 1ª opción es el titular con su nombre
✅ d3 aparecen los 2 apoderados vigentes
✅ d4 existe la opción "Otro"
✅ d5 el modal se abrió
✅ d6 las 6 líneas tildadas quedaron capturadas
✅ d7 elegir titular precarga su nombre
✅ d8 elegir titular precarga su documento  (doc="36384455")
✅ d9 elegir un apoderado precarga nombre y documento del apoderado
✅ e1 "Otro" limpia los campos para tipear de cero
✅ e2 los inputs NUNCA se deshabilitan (el select precarga, no bloquea)
✅ e3 el HTML de los inputs no tiene readonly ni disabled
✅ e4 sin nombre NO se emite (validación previa al RPC)
✅ f1 se emitió el recibo con un cobrador que NO estaba en la lista
✅ f2 cobrador_nombre persistido NO es null  (="SERENO SIN APODERADO")
✅ f3 cobrador_documento persistido NO es null  (="11222333")
✅ f4 forma_pago = efectivo
✅ f5 el modal se cerró tras emitir
✅ f6 las 6 líneas quedaron pagadas contra ese recibo  (n=6)
✅ c1 se generan 2 copias (original + duplicado)
✅ c2 rotuladas ORIGINAL y DUPLICADO
✅ c3 cada copia trae las 6 filas
✅ c4 hay 2 bloques .recibo-pie
✅ c5 el TOTAL está dentro del pie
✅ c6 "Retira" está dentro del pie
✅ c7 la FIRMA está dentro del pie (no suelta al final de la copia)
✅ c8 el pie imprime el nombre del cobrador que no era titular
✅ c9 el pie imprime el documento
✅ c10 el encabezado dice "A nombre de" con el beneficiario
✅ h1 si cobra el titular, el pie dice "el titular"
✅ h2 y NO repite el nombre del titular en el pie  (pie=483b)
✅ h3 el nombre del titular sigue estando arriba, en "A nombre de"
✅ g1 en transferencia se muestra el campo de comprobante
✅ g2 la nota del modal avisa que no hay firma
✅ g3 se emitió con forma_pago = transferencia
✅ g4 el comprobante quedó persistido
✅ g5 el cobrador también se registra en transferencia
✅ g6 en transferencia el recibo NO imprime el bloque de firma
✅ g7 imprime la nota de transferencia
✅ g8 imprime la referencia del comprobante
✅ g9 y el pie sigue siendo un bloque atómico
✅ g10 en efectivo sí imprimía firma (control de la comparación)
✅ R1 cleanup: fixtures borradas (liquidación, líneas y recibos de prueba)
✅ R2 cleanup: no quedan líneas huérfanas de la liquidación de prueba
✅ R3 restore por ESTADO: el sandbox quedó campo por campo como estaba  (sin diferencias)
✅ R4 el probe no pisó ninguna línea ajena (0 restauraciones de emergencia)  (0 línea(s) hubo que devolver a su estado)
✅ R5 no quedó ningún recibo creado durante la corrida, en NINGÚN club
✅ R6 club_secuencias de Dolores devuelto a donde estaba  (antes=32 después=32)

✅ TODO OK — 57 checks
```

```
$ echo "exit=$?"
exit=0
```

**R6 pasa: `antes=32 después=32`.** El probe consumió los números 33 y 34 durante la
corrida y los devolvió. Sin el fix, el contador habría quedado en 34.

---

## 3 · El comentario corregido en `probe_anular_recibo.mjs`

```diff
-    // El probe emite contra el club B (Mi Club Hípico) a propósito, para no correr el
-    // correlativo de Dolores. Igual se devuelven las dos secuencias a donde estaban.
+    // El grueso de los recibos sale contra el club B (Mi Club Hípico) para no mover el
+    // correlativo de Dolores, PERO el fixture detClubA emite uno contra Dolores: el candado
+    // de club (P6/P7) necesita un recibo ajeno de verdad para verificar que un usuario del
+    // club B no lo puede anular. O sea: el probe toca las DOS secuencias, y por eso las
+    // snapshotea y las devuelve a las dos. No decir acá que "sólo emite contra el club B":
+    // era falso y el que lo leyera iba a confiar.
```

El código no se tocó: el fixture de Dolores es necesario para el candado de club.

---

## 4 · ISSUE-056 en `docs/ISSUES.md` — cerrado PARCIAL

`Estado: 🟡 **PARCIAL** (2026-08-30) — RPC vivo en prod, UI pendiente.`

Los 5 requisitos que habían salido del revert del recibo #4 se contestan uno por uno en el
doc. El punto 2 se resolvió **distinto a como estaba planteado**: en vez de persistir el
estado previo en una columna nueva, el RPC lo deriva de `fecha_liberacion` de la propia
línea (futura → `retenido`, si no → `impago`). Y se agregó algo que no estaba en la lista,
`recibos.lineas_anuladas jsonb`, porque `liquidacion_detalle` no tiene trigger de auditoría
y sin esa foto el contenido de un recibo anulado se pierde para siempre.

Lo que falta quedó escrito explícito, no como omisión: **el tab Pagos no tiene botón de
anular ni formulario de motivo**, así que hoy el RPC sólo se invoca desde la consola. El
agujero operativo del 20/09 —Valeria anulando sola, con gente en la ventanilla— sigue
abierto, aunque ya no requiera escribir SQL a mano.

---

## 5 · git — salida cruda

```
$ git log --oneline --graph -8
*   80839ff merge: el protocolo de informes cubre toda salida y exige push a origin
|\  
| * d6e6eba docs: un informe no está entregado hasta que está en origin
| * d9e58cf docs: el protocolo de informes cubre toda salida, no sólo los diagnósticos
* |   34f6e83 merge: ISSUE-056 — anular_recibo v1 (RPC vivo en prod, UI pendiente)
|\ \  
| |/  
|/|   
| * c5c599c docs+test: cierre parcial de ISSUE-056 y probes que devuelven el correlativo
| * f19919e feat: anular_recibo v1 (ISSUE-056) — RPC + rollback + probe
|/  
*   cbeeee8 merge: ISSUE-059/060/057 — aislamiento entre clubes en el circuito de cobro
|\  
| * 59e278b docs: cierre de ISSUE-057/059/060 + GOTCHAS #79 y #80

$ git rev-parse HEAD
80839ff65a804b2b1843411ea93284891c85e74a

$ git ls-remote origin main
80839ff65a804b2b1843411ea93284891c85e74a	refs/heads/main

$ git show --stat --oneline 34f6e83
34f6e83 merge: ISSUE-056 — anular_recibo v1 (RPC vivo en prod, UI pendiente)

 CHANGELOG.md                             |  57 ++++
 docs/ISSUES.md                           |  55 +++-
 migrations/anular_recibo_v1.sql          | 180 ++++++++++++
 migrations/rollback_anular_recibo_v1.sql |  16 ++
 tests/probe_anular_recibo.mjs            | 452 +++++++++++++++++++++++++++++++
 tests/probe_recibo_pie_cobrador.mjs      |  27 +-
 6 files changed, 779 insertions(+), 8 deletions(-)

$ git show --stat --oneline 80839ff
80839ff merge: el protocolo de informes cubre toda salida y exige push a origin

 CLAUDE.md | 31 +++++++++++++++++++++++++++----
 1 file changed, 27 insertions(+), 4 deletions(-)

$ git ls-tree -r origin/main --name-only | grep -E "anular_recibo|probe_anular"
migrations/anular_recibo_v1.sql
migrations/rollback_anular_recibo_v1.sql
tests/probe_anular_recibo.mjs

$ git status
On branch main
Your branch is up to date with 'origin/main'.

nothing to commit, working tree clean
```

Sólo cambiaron `.md`, `.sql` y `.mjs`: **ningún HTML**, así que este merge no cambia nada
de lo que sirve GitHub Pages y no hay que verificar deploy contra `sigh.com.ar`.

---

## 6 · Números de resumen

| | |
|---|---|
| SHA del merge de ISSUE-056 | `34f6e83` |
| SHA del merge del protocolo | `80839ff` |
| `main` en origin | `80839ff` ✔ verificado con `git ls-remote` |
| Archivos que entraron | 7 (2 migraciones, 2 probes, ISSUES.md, CHANGELOG.md, CLAUDE.md) |
| `probe_recibo_pie_cobrador` | 57/57, R6 `antes=32 después=32` |
| `club_secuencias` Dolores | 32 — sin tocar, por decisión |
| ISSUE-056 | 🟡 PARCIAL — RPC sí, UI no |
| Ramas mergeadas | `feat/anular-recibo`, `chore/protocolo-informes-salidas` (no borradas) |

---

## 7 · Queda pendiente

1. **UI de anulación en el tab Pagos de `liquidaciones.html`** — entrega siguiente,
   prioridad alta antes del 20/09. Es lo único que separa a ISSUE-056 de cerrado.
2. **La reunión de prueba 9999 (PRUEBA RESUMEN) sigue viva en Dolores.** Los probes la
   usan de sandbox. Hay que borrarla con `teardown_prueba_resumen_9999.sql` antes de la
   reunión real, y eso deja sin sandbox a `probe_recibo_pie_cobrador`.
3. **Las dos ramas mergeadas no se borraron**, por si querés revisar el historial lineal.
